#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
cin>>n;
char arr[n];
for(int i=0;i<n;i++) 
 cin>>arr[i];
        
        int count = 0;
        int altitude = 0;
        
   for(int i=0; i<n;i++) 
  {
    if(arr[i]!='U' || arr[i]! ='D') 
{
       cout<<"Please provide valid inputs";
exit(0) ;
}
  }
        for(char c : hike) {
            // Step up
            if(c == 'U') {
                if(altitude == -1) {
                    count++;
                }
                altitude++;
            }
            // Step down
            else {
                altitude--;
            }
        }       System.out.println(count);
    }
}
    return (0);
}
